# Release 0.1.6

User authorized a Release build and publication to GitHub, plus separate
commands for updating version files and building/uploading the release.
Latest existing release: v0.1.5. Repository is public.

Plan:
1. Extend the existing version JS to update four version files, including
   Cargo.lock, with explicit/patch/minor/major and read-only check modes.
2. Make publishing use the current committed version, remove destructive
   checkout restoration and embedded GitHub credentials, and publish only
   after signed assets and the updater manifest are complete.
3. Verify the version script, stamp 0.1.6, commit scoped release changes,
   build from a clean worktree, and publish the release.

Includes the Unit Model Editor fixes verified by six focused regressions in
`2026-09-13-unit-model-extract-replace.md`. Uncommitted MSC research work in the
main checkout must remain untouched and outside the build snapshot.

Implementation complete. Five version-script tests passed with
`node --test .github/scripts/stamp-release-version.test.mjs`.
`pnpm version:bump 0.1.6` synchronized all four version files.
Release build and publication pending.
