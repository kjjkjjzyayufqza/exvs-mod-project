---
alwaysApply: true
---

以下规则在本项目中**全局适用**：

1. 本项目基于 **Tauri v2** 实现。
2. 只要涉及读取/写入文件、访问本机资源、或任何浏览器环境以外的本机能力，必须优先使用 **Tauri v2 的 API/插件**实现（例如 `@tauri-apps/plugin-fs`、`@tauri-apps/plugin-dialog` 等）。不要直接依赖 Node.js 的 `fs`、`path` 等能力来访问本机文件系统。
3. 编写任何代码时，只要你判断可能触发性能问题（例如：大文件读写、频繁重渲染、深层递归、巨量列表渲染、重复解析/序列化、阻塞主线程等），必须**立刻**与用户沟通风险点与备选方案，并在得到确认后再实现（例如：分块读取、Web Worker、节流/防抖、虚拟列表、缓存、懒加载等）。
4. 编写任何组件或页面时，必须遵循本项目既有的代码风格与目录风格：每个 `page` 组件都应有其对应的 `components` 目录用于拆分子组件，避免在单一文件中堆叠过多 UI/逻辑。

补充的全局约束（建议）：

5. **输出与代码语言**：与用户沟通时使用中文；生成的代码与代码注释使用英文；不要在代码中出现中文（包括 Python 代码）。
6. **禁止 TODO**：不要在代码中留下 `TODO` / `FIXME` 等占位标记；若有未确定事项，必须在实现前询问用户并落地为可执行方案。
7. **不要启动 dev server**：不要运行 `npm dev` / `pnpm dev` 等启动服务的命令，除非用户明确要求。
8. **一致性优先**：新增功能/修改逻辑时，优先复用现有工具函数、组件与数据模型；避免引入重复实现；文件命名、导出方式、目录组织保持与周边代码一致。
9. **useTransition（React）性能优化**：当用户交互会触发较重的 UI 更新（例如：大列表渲染、复杂计算/过滤/排序、树结构重建、图表/3D 视图重算）且该更新不需要阻塞输入/点击反馈时，优先将对应的 `setState`/路由切换包进 `startTransition`（或使用 `useTransition`）以降低卡顿；但**不要**用 `useTransition` 替代 I/O 的等待（例如文件读写/网络请求的 `await`），I/O 的 loading/错误处理仍应使用显式状态管理（如 `isLoading`）。
