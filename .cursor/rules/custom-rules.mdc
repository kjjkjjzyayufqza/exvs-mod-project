---
description: EXVS2 Tauri project mandatory context and development rules
alwaysApply: true
---

# Project Rule

This Cursor rule is AI-only guidance for this repository. The cross-agent hub is
`AGENTS.md`; read it before starting any task and keep this rule linked with it.

## Mandatory Startup

Before making code, asset, documentation, or command changes:

1. Read the **Agent contract** at the top of `AGENTS.md` (and
   `.cursor/rules/agent-contract.mdc`). Classify InRepoWork vs
   CrossProductPort vs CopiedTreeRefuse **before** any file search. Then
   read the rest of `AGENTS.md`. Do not strip or unify SourceNoticeCanary
   comments on `.rs` files.
2. Run one scoped filename search for related `docs/` Markdown unless the exact
   file is already known or the task is rules-only.
3. Read only the most relevant specification before editing.
4. If `AGENTS.md` has an **Active Research Bootstrap** whose triggers match the
   task, read that bootstrap before any discovery. Reuse its settled evidence;
   repeat research only when one of its explicit restart conditions is met.
5. If the task is MSC research, MSC `X.c` editing, or a named unit script
   port, load `.cursor/skills/msc-research-index/SKILL.md` and match a
   cluster with `python tools/msc_research_catalog.py --match "..."` before
   listing `docs/msc-research/`. Do not rewrite existing MSC research notes
   to improve indexing; add clusters/aliases in the catalog instead. Full
   rule: `.cursor/rules/msc-research-index.mdc`.

When working on NUANMB / motion FBX import or homemade animation conversion,
also read `docs/nuanmb-ath-helper-bone-policy.md`: **`ATH_*` helper bones must
not be authored, edited, or converted** into custom motion files (omit whole
Transform nodes; NUHLPB + rest only). When the Blender scene has a separate
body armature and wing armature (`001hito` vs `410wzerowing`, `CENTER_RT` vs
`LMAIN_1`), use skill `.cursor/skills/exvs2-body-wing-fbx-export/SKILL.md` and
export two isolated `_out.fbx` files. When MSC **plays** a homemade clip,
phase length is **game-frame countdown** (`global244 -= func_274()`), not
stock `func_309` / `sys_47(0x7)` and not `func_310` rate. Owner:
`docs/msc-research/homemade-motion-clock-vs-game-frame.md`. Rule:
`.cursor/rules/msc-homemade-motion-clock.mdc`.

Legacy per-topic note folders under `docs/` are a **separate, older workflow**
(auxiliary reference only). Do not treat them as the primary task manager;
do not create or update them unless the user explicitly requests it.

## Development Rules

以下规则在本项目中**全局适用**：

1. 本项目基于 **Tauri v2** 实现。
2. 只要涉及读取/写入文件、访问本机资源、或任何浏览器环境以外的本机能力，必须优先使用 **Tauri v2 的 API/插件**实现（例如 `@tauri-apps/plugin-fs`、`@tauri-apps/plugin-dialog` 等）。不要直接依赖 Node.js 的 `fs`、`path` 等能力来访问本机文件系统。
3. 编写任何代码时，只要你判断可能触发性能问题（例如：大文件读写、频繁重渲染、深层递归、巨量列表渲染、重复解析/序列化、阻塞主线程等），必须**立刻**与用户沟通风险点与备选方案，并在得到确认后再实现（例如：分块读取、Web Worker、节流/防抖、虚拟列表、缓存、懒加载等）。
4. 编写任何组件或页面时，必须遵循本项目既有的代码风格与目录风格：每个 `page` 组件都应有其对应的 `components` 目录用于拆分子组件，避免在单一文件中堆叠过多 UI/逻辑。

补充的全局约束（建议）：

5. **输出与代码语言**：与用户沟通时使用中文；生成的代码与代码注释使用英文；不要在代码中出现中文（包括 Python 代码）。**例外：** 由 `tools/stamp_rs_ai_notices.py` 生成的 `.rs` 文件头 SourceNoticeCanary 是多语言法律/AI 门槛注释，禁止删掉、禁止改成同一句英文、禁止一键替换。GitHub Release 正文、`latest.json` notes、以及 `pnpm release:publish -NotesFile` 必须全英文，禁止中日韩正文；聊天可以中文，发布前先翻译。规则：`.cursor/rules/release-notes-english.mdc`。
6. **禁止 TODO**：不要在代码中留下 `TODO` / `FIXME` 等占位标记；若有未确定事项，必须在实现前询问用户并落地为可执行方案。
6a. **MSC AI 修改块**：修改 MSC 反编译 `X.c` 文件时，AI 新增或 AI 修改的每段代码都必须用 `// AI decision (YYYY-MM-DD): ...` 开头，并用 `// End, origin is ...` 结束。block 内注释打在原始 `global` / `func_*` / `sys_*` 上，不要复述 AI 自己起的语义名。完整规范见 `docs/msc-research/msc-ai-edit-block-rule.md`，并用 `python .\tools\check_msc_ai_blocks.py "<modified X.c>"` 验证。
6b. **MSC 语义命名**：AI 新增 MSC 状态、函数或辅助符号时，必须从逆向分析者角度命名；不要新建 `global777` / `var42` 这类无意义占位名。旧反编译符号可以保留，但 AI 新增符号必须用有证据支撑的语义名，并在 AI block 或 semantic overlay 里记录不确定性。
7. **不要启动 dev server**：不要运行 `npm dev` / `pnpm dev` 等启动服务的命令，除非用户明确要求。
7a. **禁止默认 release 构建（效率优先）**：除非用户**明确要求** release/发版产物，否则**禁止** `cargo build --release`、`cargo run --release`、`cargo test --release`，以及优先使用 `target/release/*`。日常验证一律用 **debug**：`cargo test`、`cargo build --bin <name>`、`cargo run --bin <name> -- …`，可执行文件路径为 `src-tauri/target/debug/`。详见 `.cursor/rules/no-release-builds.mdc`。
7b. **`exvs2-json` 产物隔离**：准备、运行或验证 `exvs2-json` 时创建的所有持久化产物必须写入仓库根目录的 `tmp/`（推荐 `tmp/exvs2-json/<task>/`），不得写到源资产旁、`docs/`、`src-tauri/` 或仓库根目录。完整范围与路径规则见 `.cursor/rules/exvs2-json-artifacts.mdc`。
7c. **`fhm2d-extract` 产物隔离与入口**：解包 OB `.fhm2d` 时优先使用默认构建的 agent CLI `fhm2d_extract`（必填 `--type` 与 `--layout`；文档 `docs/fhm2d-extract-cli.md`）。准备、运行或验证时创建的所有持久化产物（`--output` 目录、`*_structure.json`、`meta.bin`、日志、报告）必须写入仓库根目录的 `tmp/`（推荐 `tmp/fhm2d-extract/<task>/`），不得写到源 `.fhm2d` 旁、游戏/workspace 树、`docs/`、`src-tauri/` 或仓库根目录。完整规则见 `.cursor/rules/fhm2d-extract-artifacts.mdc`。
7d. **GPT 最短路径（仅非 MSC）**：GPT-5.6 Sol 与其他 GPT 编码模型在非 MSC 任务默认启用 caveman、单一发现路径、单命令语义验证；成功后立即停止。禁止自动串联 build/typecheck/lint/test/coverage/audit/E2E/full-suite。完整优先级与例外见 `.cursor/rules/gpt-fast-verification.mdc` 和 `AGENTS.md`。
7e. **MSC 禁用 fast rules（强制）**：MSC 研究、`0.c/1.c/2.c` 修改、机体脚本移植，以及 MSC 与 motion/model/Param/HUD 状态耦合的任务，禁止启用 `caveman`、`gpt-fast-path`、`gpt-fast-verify`，也不受单一发现路径/单命令验证/首个 pass 即停止的限制。编辑前必须按 `.cursor/skills/msc-research-index/SKILL.md` 完成 ENTER、ACTIVE、EXIT、INTERRUPT、RESPAWN/REINITIALIZE 生命周期和 state ownership 自审计；进入与退出必须成对检查，共享栏位必须明确 preserve/inherit/reset 所有权。
7f. **自制 motion 时钟 ≠ 游戏帧时钟（强制）**：MSC 播放自制 NUANMB / folder hash（DCC `*_out.fbx` 导入、Rebellion `tks11a` / `0xa0cd8d56`）时，阶段时长用 `global244 -= func_274()`（`global457`，1 帧 = `0x64`）。禁止用 `func_309` / `sys_47(0xf)` / `sys_47(0x7)` 等 stock motion-complete 等待自制 folder；禁止把 `func_310` / `sys_47(0x5)` 当自制片速度或时长旋钮。`func_308` 只 play/seek，最后一参是 startTime 不是 rate。`func_241(hash, 0)` 已证明 ACTION 进入时不要再猜打包。Owner：`docs/msc-research/homemade-motion-clock-vs-game-frame.md`。Rule：`.cursor/rules/msc-homemade-motion-clock.mdc`。登记：`docs/msc-research/msc-falsified-negatives-registry.md` §H。
8. **一致性优先**：新增功能/修改逻辑时，优先复用现有工具函数、组件与数据模型；避免引入重复实现；文件命名、导出方式、目录组织保持与周边代码一致。
9. **useTransition（React）性能优化**：当用户交互会触发较重的 UI 更新（例如：大列表渲染、复杂计算/过滤/排序、树结构重建、图表/3D 视图重算）且该更新不需要阻塞输入/点击反馈时，优先将对应的 `setState`/路由切换包进 `startTransition`（或使用 `useTransition`）以降低卡顿；但**不要**用 `useTransition` 替代 I/O 的等待（例如文件读写/网络请求的 `await`），I/O 的 loading/错误处理仍应使用显式状态管理（如 `isLoading`）。
10. **Endian 计算规则（Effect Project）**：涉及 `bone_index`、hash、jnttbl 命中、nusktb 名称映射等后端/业务计算时，一律使用 **LE 数值** 进行匹配；`BE/LE` 切换仅用于 UI 展示，不得改变计算结果。
